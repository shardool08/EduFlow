// Supabase client — mocked until credentials are configured.
// Replace with real initialization once EXPO_PUBLIC_SUPABASE_URL
// and EXPO_PUBLIC_SUPABASE_ANON_KEY are set in .env.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const supabase = null as any;
